import 'package:flutter/cupertino.dart';

class ITrueViewPage extends StatelessWidget {
  const ITrueViewPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('true view page'),
    );
  }
}
