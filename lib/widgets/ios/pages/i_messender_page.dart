import 'package:flutter/cupertino.dart';

class IMessengerPage extends StatelessWidget {
  const IMessengerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('messenger page'),
    );
  }
}
