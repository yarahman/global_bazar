import 'package:flutter/material.dart';

class TrueViewPage extends StatelessWidget {
  const TrueViewPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('true view page'),
    );
  }
}
